/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',

  basePath: '/redus-geo-poc',
  assetPrefix: '/redus-geo-poc/', // assetPrefix requires the trailing slas
}

module.exports = nextConfig
